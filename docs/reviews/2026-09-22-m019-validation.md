# M019 validation — 2026-09-22

Branch: `codex/m019-common-actor`; implementation e3143ce, based on main b232dce. Manual M019 acceptance remains pending. Main is not merged.

## Automated results

`bash tools/check_godot.sh` exited 0 using Godot 4.7.2 stable on Windows via WSL. Editor parse/import, default main scene startup and all 20 test scripts passed. [Raw output](2026-09-22-m019-check.txt).

| Script | Result |
| --- | --- |
| `test_basic_interaction.gd` | PASS |
| `test_combat_body.gd` | PASS |
| `test_combat_log.gd` | PASS |
| `test_common_action_system.gd` | PASS |
| `test_common_actor.gd` | PASS |
| `test_eight_way_combat.gd` | PASS |
| `test_generated_map_combat.gd` | PASS |
| `test_generation_settings.gd` | PASS |
| `test_m017_m018_integration.gd` | PASS |
| `test_micro_ap_game.gd` | PASS |
| `test_multiple_npcs.gd` | PASS |
| `test_procedural_map_preview.gd` | PASS |
| `test_procedural_playground.gd` | PASS |
| `test_seed_deriver.gd` | PASS |
| `test_simple_map_generator.gd` | PASS |
| `test_single_actor_replay.gd` | PASS |
| `test_tactical_ai.gd` | PASS |
| `test_time_cost_game.gd` | PASS |
| `test_time_cost_input.gd` | PASS |
| `test_time_scheduler.gd` | PASS |

## New coverage and compatibility

- `test_common_actor`: shared definitions, independent HP/abilities/body/species overrides/AI/facing/targets; arbitrary IDs; duplicate/occupied registrations; stable order; missing IDs; terrain/past-time rejection; coordinated creation, death and removal; human NPC action costs; legacy adapters.
- `test_multiple_npcs`: player-first/NPC-stable ties, per-ID costs, unknown-policy waits, invalid-free actions, NPC occupancy, blocked-door diagonals, injuries isolated between siblings, multi-attacker damage, arbitrary target death, no dead actions, freed tiles, player death, fear/aggression isolation, disabled/blocked wait and restored routing, deterministic reachable placement/reset/atomic rejection/small maps, repeated per-input replay without live overlaps. Actual scene input exercises three moves, three attacks, one rat's retreat and bump death plus readable status/log bounds.
- `test_single_actor_replay`: SHA-256 fixture captured from main b232dce before changes, for 3 seeds × up to 30 inputs. Canonical positions/HP/body parts/events/AI choices/clock/RNG state reproduce exactly. Capture is explicit `-- --capture`; ordinary tests never update expected data. This fixture supplements, rather than replaces, existing semantic assertions.
- Existing tests retain 17 scripts. Only old generated single-rat fixtures explicitly request `configure(rows, 1)` where response counts assume one NPC. The actual generated scene remains at the default three; scene comparison uses count 3. Scheduler, body rules and terrain generation code were not changed.

## Presentation evidence and limits

Ran `tools/capture_m019_scene.gd` with the Windows Godot renderer (`--rendering-method gl_compatibility`, not headless). [Capture](2026-09-22-m019-scene.png) was inspected: markers 1/2/3, independent HP/body/cost rows and six ability controls fit the 1152×648 viewport; panel and log are separate. The renderer reported no errors. The automated input tests cover state transitions and label updates, not subjective pacing or interactive tooltip ergonomics.

No manual Godot playthrough, export build, save/load, perception or larger-roster/small-window validation is claimed. Manual checks are listed in [M019](../milestones/M019_common_actor.md). Existing unrelated untracked tileset import/review files were preserved and excluded from commits.

## Review conclusions

No design principle or temporary number required changing. IDs identify instances; type and policy derive from definitions/Actor data. Registry has no duplicate HP/body store; compatibility dictionaries contain object references only. Registration rollback and dead scheduling were reviewed. Generated spawn registration executes outside assertions so release builds do not omit NPC creation. Unexpected execution failure stops the simulation instead of silently charging time; normal missing options produce a scored Wait.

Known follow-ups: manually assess three-rat pacing; replace first-rat adapters when legacy fixtures migrate; scrolling/selection for larger rosters; shared-definition immutability remains a content-authoring convention. Future gameplay systems remain separately scoped in the roadmap.
