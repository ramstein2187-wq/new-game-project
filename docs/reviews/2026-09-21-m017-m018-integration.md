# M017/M018 integration — 2026-09-21

## Authorization and source selection

- User reported M017 manual verification complete and requested integration with M018. User explicitly selected latest `chat/diagonal-movement-cost` (diagonal movement costs 1.4×).
- User subsequently clarified that **both M017 and the latest M018 version passed their manual verification**. This is user-reported sign-off; the agent's corner correction and panel clarification have automated integration coverage, not a newly claimed user playthrough.
- M017 source: `9418443`. M018: `980f05f` followed by diagonal-cost commit `03c13ef`. Both descend directly from M017; no content merge conflict.
- Alternative `chat/eight-direction-combat` (`6c9a854`) is an independent implementation, not a dependency of the selected branch. Do not merge duplicate implementations blindly.
- Main baseline is `ac89eaf`. Work is on `codex/integrate-m017-m018`. Existing unrelated untracked imports/review files are preserved.

## Investigation / resume

- Reviewed AGENTS, roadmap, milestone index, M017/M018 docs, turn-time decision, both M018 implementations, action/input/AI changes and tests.
- Preserve cardinal 1000/750 and diagonal 1400/1050 movement costs. Apply body locomotion efficiency before final ceil; attack and wait costs unchanged.
- Suspected defect in selected M018: AI uses Chebyshev adjacency without corner reach, so a blocked diagonal opponent can produce Wait forever despite a legal route. Verify with regression before changing behavior.
- Check actual keypad inputs in both scenes, blocked generated-map corners, diagonal injury costs and scheduler/log accounting. Existing cardinal-only spawn connectivity remains a conservative guarantee.
- Integration validation, implementation decisions and final delivery status will be appended here. M017 manual approval is user-reported; do not claim the agent performed a GUI playthrough or that untested integration fixes were manually approved.

## Reproduced and corrected

- Original selected branch passed its existing 16 tests ([source validation](2026-09-21-m018-source-validation.txt)). Added an integration test before changing production code: six assertions failed for corner routing and false striking-range feedback ([reproduction](2026-09-21-m018-reproduction.txt)).
- Shared `can_melee_reach` now drives AttackAction, RatTactics approach-vs-bite and MoveAction visibility. A healthy rat walks around a blocked adjacent corner, then attacks through the existing action/scheduler pipeline. Unavailable bite still falls back safely to Wait.
- Kept selected M018 keypad mapping, collision policy and 1.4× base costs. Did not merge the competing equal-cost branch. Removed an unused retreat-distance parameter; restored exact event-order and injured approach-cost assertions that the source branch had weakened.
- The body panel now displays both cardinal/diagonal movement costs, including injury. Core clock and combat RNG are unchanged.

## Automated validation

- `bash tools/check_godot.sh`: editor parse/import, generated-map default startup and all **17 test scripts** passed on Godot 4.7.2. [Integration output](2026-09-21-m017-m018-validation.txt).
- New test covers wall/tree/ruin corner approach; truthful reach feedback; door interaction; actual generated-map player 1867 / rat 1200 injured diagonal scheduling; diagonal retreat; arm injury and disabled bite; all eight keypad directions and ignored echoes through both scene viewports; panel/log separation and visible direction costs.
- Earlier M017 500-encounter statistics are historical four-direction evidence, not current eight-direction balance results. Native Num Lock/keyboard-layout behavior and any visual behavior beyond the user's reported manual scope are not claimed as agent-tested.

## Delivery and remaining work

- Integration correction commit: `d464991`; source M017/M018 ancestry is preserved. Main delivery is tracked by [PR #4](https://github.com/ramstein2187-wq/new-game-project/pull/4), with an expected-head merge check and local fast-forward planned after GitHub confirms the merge. The PR's merge commit is the canonical main delivery reference.
- User manual acceptance covers both selected features. No separate milestone was invented for integration; M017/M018 status, design and roadmap were reconciled together. Existing unrelated untracked files are excluded.
- Follow-ups remain encounter/arm-injury balance under eight-way movement, optional weighted route choice, alternative keyboard mappings and smaller-window layout. These do not reopen the accepted implementation scope.
