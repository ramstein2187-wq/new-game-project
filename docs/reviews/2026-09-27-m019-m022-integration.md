# M019 then M022 main integration — 2026-09-27

## Scope and safety

The user explicitly authorized separate M019 then M022 main integrations and pushes. This supersedes the historical M019 merge gate, but does not establish manual play acceptance. No manual playthrough is claimed.

Refreshed origin via existing WSL SSH configuration: main `b232dce3245c6f958337e4b52c678c42589eb1d5`, M019 `a1a4b272a98802061f372e16eb55fd28857a698c`, M022 `164c8ac4eaaa99f91607e238da30260ec0c73aff`. Local refs matched. Original checkout retains its 17 unrelated untracked files; a SHA-256 baseline is saved outside the repository. Worldgen branches/M020/M021 are outside scope.

Integration checkout: `C:\GameDev\integration-m019-m022`. Managed worktree tool returned “Not a git repository” because this chat is rooted at `C:\GameDev`; used a normal dedicated Git worktree. No reset/clean/force push/history rewrite.

## M019 integration

- Base: main `b232dce`; source: `a1a4b27` (implementation `e3143ce`). Ancestry confirmed source absent from main and descended from main.
- Branch: `codex/integrate-m019`; normal integration merge: `97c51f669108a3669772b0123fc0e027c73386eb`. Its tree exactly matches the M019 source; no conflict or game-code adjustment.
- Reviewed ActorDefinition/Actor/ActorRegistry ownership, isolated abilities/body/species/AI/position/facing, occupancy/targets, multiple scheduling, default three generated NPCs, named logs, death and reset. Combat rules, scheduler and terrain generation retained.
- Godot `4.7.2.stable.mono.official.ed1daf0bf`: `bash tools/check_godot.sh` exited 0; editor import/parse, main startup and **20/20 scripts passed**, including M019 focused tests and pre-M019 single-rat replay over three seeds. [Raw evidence](2026-09-27-m019-integration-check.txt).
- Documentation/PR head: `1032e77b1c5cca0b3357f11a10f8a4fc1fb5fb05`. [PR #7](https://github.com/ramstein2187-wq/new-game-project/pull/7) merged at `b109ffde0041f98b2ea2444665da9bc431b4e8ef`; local main fast-forwarded and exactly matched the validated integration tree before M022 began. Manual pacing/readability acceptance remains unverified.

## M022 integration

- Base: updated main `b109ffd`; source `164c8ac4eaaa99f91607e238da30260ec0c73aff`, one commit above M019 `a1a4b27`.
- Branch: `codex/integrate-m022`; normal merge `857374fc5e6590605092fcf7b3af607850320ff2`. Resolved only ROADMAP/MILESTONES status conflicts. Code/tests/tools/procgen exactly match source; M019 changes are already in main and excluded from this PR delta.
- Reviewed production reuse: `CombatSimulationGame` supplies actors/targets only; `choose_ai_action` extracts the original NPC policy without executing or scheduling. Both sides select via RatTactics and execute TimeAction through `perform_action`, producing CombatEvents. No duplicated combat/body/armor/movement/AI rules. NPC semantics remain select then execute.
- Event log is consumed per scheduling window to avoid presentation retention truncation; damage counts actual HP loss from remaining_hp. Contiguous seeds, separate error/stall/win outcomes and guards retained.
- Godot 4.7.2: full import/parse/startup and **21/21 scripts passed** at 857374f, including M019 regressions, single-rat replay and deterministic batch metrics/termination.
- Both actual CLI runs exited 0. [21-script check output](2026-09-27-m022-integration-check.txt), [100-run output](2026-09-27-m022-batch-100.txt), [1000-run output](2026-09-27-m022-batch-1000.txt).

Command: `Godot --headless --path <integration> --script res://tools/run_combat_batch.gd -- --runs=N --base-seed=0 --max-actions=500 --max-world-time=500000`.

| Metric | 100 runs | 1000 runs |
| --- | ---: | ---: |
| Seed range | 0..99 | 0..999 |
| Requested / completed / errors | 100 / 100 / 0 | 1000 / 1000 / 0 |
| Side A wins / side B wins / stalled | 12 / 14 / 74 | 155 / 137 / 708 |
| Mean actions | 379.990 | 364.889 |
| Mean world time | 180644.680 | 172292.902 |
| Mean damage A / B | 15.230 / 15.590 | 16.159 / 15.329 |
| A move / attack / interact / wait totals | 17257 / 771 / 100 / 792 | 164131 / 8251 / 1000 / 9381 |
| B move / attack / interact / wait totals | 16941 / 780 / 0 / 1358 | 161453 / 7752 / 0 / 12921 |
| Wall time | 14.514 s | 112.877 s |
| Throughput | 6.89/s | 8.86/s |

Deterministic metrics reproduce the previous source-branch sample exactly. Timings are machine-dependent and excluded from deterministic data. Initial direct PowerShell runs produced empty redirected output; only the captured WSL reruns above supply evidence. The 100-run timing overlapped an initial unrecorded process, while the captured 1000-run sample started after it exited; do not treat the timing difference as a regression.

## Final main validation and delivery

- M022 documentation/PR head: `d245721b4071c29e3cd369004e09722d1b706dd3`. [PR #8](https://github.com/ramstein2187-wq/new-game-project/pull/8) merged normally at `542f5347202b6d656c4a96ee1ef2cbbc240aeca7`. GitHub listed 18 changed files containing M022 delta and integration docs; no M019 code repeated as a new feature.
- Final main code integration commit: `542f5347202b6d656c4a96ee1ef2cbbc240aeca7`. Local main/origin main matched; both feature source tips are ancestors and the tree exactly matches validated `codex/integrate-m022`.
- On that main: `bash tools/check_godot.sh` exited 0 with import/parse/startup and **21/21 scripts passed**, no error/failure diagnostics. [Final main check](2026-09-27-main-check.txt).
- Actual final-main CLI smoke: 20 runs, seeds 0..19, caps 500/500000; completed 20, A 2, B 0, stalled 18, errors 0; mean actions 454.000, world time 217810.700, damage A/B 16.450/13.750; 2.675 s, 7.48/s. [Raw smoke output](2026-09-27-main-smoke20.txt).
- Both integration branches were pushed with existing WSL SSH credentials; no security changes. GitHub returned no statuses/workflow runs/reviews, so local automated logs are the validation evidence.
- Original checkout remained on M019 `a1a4b27`: all 17 unrelated untracked files matched their initial SHA-256; no tracked/staged changes. Source branch tips and both worldgen branches are unchanged. Test-generated import sidecars in the isolated checkout are excluded from commits and preserved outside it.
- This documentation-only closure updates both milestone/roadmap states to Complete on main and preserves the manual-play limitation. Its subsequent final commit is recorded in local `C:\GameDev\m019-m022-integration-progress.md`, avoiding a self-referential commit hash inside this file.

## Known limitations and follow-ups

- Side A uses the scheduler player slot and wins ready-time ties. No priority compensation or scheduler change.
- Identical rat definitions do not make the fixed-room scenario symmetric: door and initial positions differ. Side win rates are infrastructure evidence, not a direct species balance claim.
- High stalled rate measures current AI/body/combat rules under the declared limits. No AI or balance tuning was performed during integration.
- Action limit is checked between scheduling windows. A side-A action resolves NPC responses too, so max_actions=500 can slightly overshoot (for example 501). Exact per-action stepping is a separate future experiment requirement.
